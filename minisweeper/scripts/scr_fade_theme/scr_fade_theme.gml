var _map = argument0;
scr_fade_colors(
	_map[? "solidColor"],
	_map[? "backColor"],
	_map[? "clearedColor"],
	_map[? "textColor"],
	_map[? "buttonTextColor"],
	_map[? "mineColor"],
	_map[? "buttonColor"],
	_map[? "flagColor"],
	_map[? "cardColor"]
);